#!/usr/bin/env sh

set -e

test "$AWS_BUCKET" != ""
test "$AWS_ACCESS_KEY_ID" != ""
test "$AWS_SECRET_ACCESS_KEY" != ""

for file in $(find -name "*.gem"); do
  aws s3 cp "${file}" s3://$AWS_BUCKET/gems/
done
